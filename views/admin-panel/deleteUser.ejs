<%- include('../partials/adminHead.ejs') %>


<div class="jumbotron">
    <div class="row">
        <div class="col-8">
            <h4 class="font-weight-bold"><%= user.username %></h4>
            <p class="lead">Alert! do you want to delete this user??</p>
            <p class="lead">
            <form action="/admin/deleteuser/<%= user._id %>" method="POST">
                <button class="btn btn-danger btn-lg" type="submit">Delete</button>
                <a class="btn btn-light btn-lg" href="/admin/allusers">Cancel</a>
            </form>
            </p>
        </div>
        <div class="col-4">
            <h1 class="font-weight-bolder text-danger float-right">
                <i class="fa fa-trash"></i>
            </h1>
        </div>
    </div>    
</div>